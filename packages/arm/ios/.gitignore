# Pulse and VoiceInputCore are small Swift packages required by every clean
# iOS/macOS checkout. Keep SwiftPM output out of source snapshots.
Vendor/*
!Vendor/Pulse/
!Vendor/VoiceInputCore/
Vendor/**/.build/
Vendor/**/.swiftpm/
