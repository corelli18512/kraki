{
  "name": "@kraki/tests",
  "version": "0.1.0",
  "private": true,
  "description": "Integration tests for Kraki — head ↔ tentacle ↔ app",
  "type": "module",
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "test:live": "vitest run --config vitest.live.config.ts"
  },
  "dependencies": {
    "@kraki/protocol": "workspace:*",
    "@kraki/crypto": "workspace:*",
    "kraki-relay": "workspace:*",
    "kraki": "workspace:*",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.2.0",
    "@types/ws": "^8.5.0"
  }
}
